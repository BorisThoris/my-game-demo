# Dependencies

- **phaser3-juice-plugin** is pinned to a specific commit in `package.json` for reproducible builds (upstream has no npm releases; the GitHub ref is fixed so production builds don’t break if the repo changes).
