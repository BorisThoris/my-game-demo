/**
 * Replace with real URLs before Steam / public ship.
 * Empty string = in-game shows "not configured" (no broken https://# clicks).
 */
export const EXTERNAL_LINKS = {
  privacy: "",
  support: "",
  store: "",
  /** Optional: public bug tracker */
  reportBug: ""
};
